# Required: JWT signing and the MCP API key index derive from this value.
PAYLOAD_SECRET=change-me
DATABASE_URI=file:./.data/mcpx.db

# Optional: absolute base URL. The "Connect a client" tab prints the endpoint
# URL from it. Defaults to http://localhost:3000.
PAYLOAD_URL=http://localhost:3000
